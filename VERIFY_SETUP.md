# ✅ সেটআপ ভেরিফিকেশন চেকলিস্ট

## 🔍 সবকিছু ঠিক আছে কিনা চেক করুন:

### ১. ডাটাবেস চেক:
```bash
cd backend
python -c "
from app import create_app, db
from app.models import User

app = create_app()
with app.app_context():
    student = User.query.filter_by(email='student@knu.edu').first()
    if student:
        print('✅ Student user exists')
        print(f'   Email: {student.email}')
        print(f'   Name: {student.name}')
        print(f'   Type: {student.user_type}')
    else:
        print('❌ Student user NOT found')
"
```

### ২. ব্যাকএন্ড API চেক:
```bash
# Terminal 1 - ব্যাকএন্ড চালু করুন
cd backend
python run.py

# Terminal 2 - API টেস্ট করুন
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"student@knu.edu","password":"student123"}'
```

**সফল রেসপন্স:**
```json
{
  "message": "Login successful",
  "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": 1,
    "name": "Student",
    "email": "student@knu.edu",
    "user_type": "student"
  }
}
```

### ৩. ফ্রন্টএন্ড চেক:
```bash
# Terminal 3 - ফ্রন্টএন্ড চালু করুন
cd frontend
npm run dev
```

**চেক করুন:**
- ✅ http://localhost:3000 লোড হয়
- ✅ http://localhost:3000/auto-login লোড হয়
- ✅ ক্রেডেনশিয়াল দেখা যায়

### ৪. লগইন টেস্ট:
```
1. http://localhost:3000/auto-login যান
2. "Login & Go to Dashboard" ক্লিক করুন
3. সাফল্য মেসেজ দেখুন
4. ড্যাশবোর্ডে যান
5. ✅ সফল!
```

---

## 📋 সম্পূর্ণ চেকলিস্ট:

### ব্যাকএন্ড:
- [ ] `python reset_db.py` চালিয়েছি
- [ ] `python run.py` চালু আছে
- [ ] http://localhost:5000 রেসপন্ড করছে
- [ ] student@knu.edu ইউজার ডাটাবেসে আছে
- [ ] পাসওয়ার্ড student123 সঠিক

### ফ্রন্টএন্ড:
- [ ] `npm install` করেছি
- [ ] `npm run dev` চালু আছে
- [ ] http://localhost:3000 লোড হয়
- [ ] /auto-login পেজ লোড হয়
- [ ] ক্রেডেনশিয়াল ফিল্ড দেখা যায়

### লগইন:
- [ ] লগইন বাটন ক্লিক করতে পারছি
- [ ] সাফল্য মেসেজ দেখা যাচ্ছে
- [ ] ড্যাশবোর্ডে যেতে পারছি
- [ ] নেভিগেশন বার দেখা যাচ্ছে
- [ ] ইউজার প্রোফাইল দেখা যাচ্ছে

---

## 🚀 দ্রুত স্টার্ট:

### একবারে সবকিছু চালু করুন:

**Terminal 1 - ব্যাকএন্ড:**
```bash
cd backend
python reset_db.py
python run.py
```

**Terminal 2 - ফ্রন্টএন্ড:**
```bash
cd frontend
npm install
npm run dev
```

**Browser:**
```
http://localhost:3000/auto-login
```

---

## ✅ সফল সেটআপের লক্ষণ:

1. ✅ ব্যাকএন্ড http://localhost:5000 এ চলছে
2. ✅ ফ্রন্টএন্ড http://localhost:3000 এ চলছে
3. ✅ /auto-login পেজ লোড হয়
4. ✅ ক্রেডেনশিয়াল দেখা যায়
5. ✅ লগইন বাটন কাজ করে
6. ✅ ড্যাশবোর্ডে যেতে পারছি
7. ✅ নেভিগেশন বার দেখা যাচ্ছে

---

## ❌ সমস্যা হলে:

1. **ব্রাউজার কনসোল খুলুন** (F12)
2. **এরর মেসেজ দেখুন**
3. **ব্যাকএন্ড লগ চেক করুন**
4. **সব সার্ভার রিস্টার্ট করুন**
5. **ডাটাবেস রিসেট করুন**

---

**সবকিছু ঠিক থাকলে অটো লগইন ১০০% কাজ করবে! ✅**
