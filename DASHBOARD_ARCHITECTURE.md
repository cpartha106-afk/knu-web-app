# 🏗️ Modern Dashboard Architecture

## সিস্টেম আর্কিটেকচার

```
┌─────────────────────────────────────────────────────────────────┐
│                     KNU MODERN DASHBOARD                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              NAVBAR (Sticky)                             │  │
│  │  [Logo] [Title] [Menu] [Theme] [Profile] [Logout]      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              SIDEBAR (Collapsible)                       │  │
│  │  • Dashboard                                             │  │
│  │  • Profile                                               │  │
│  │  • Settings                                              │  │
│  │  • Help                                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              MAIN CONTENT AREA                           │  │
│  │                                                          │  │
│  │  Welcome Section                                         │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ Welcome, [Student Name]! 👋                        │ │  │
│  │  │ Manage your academic journey with ease             │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  │                                                          │  │
│  │  Stats Cards (4 columns)                                │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │  │
│  │  │ 📚 90+   │ │ 📖 1000+ │ │ ✅ 95%   │ │ 📈 75%   │  │  │
│  │  │ Courses  │ │ Subjects │ │Attendance│ │ Progress │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │  │
│  │                                                          │  │
│  │  Main Dashboard Grid (2x2)                              │  │
│  │  ┌────────────────────┐ ┌────────────────────┐         │  │
│  │  │ 🔍 Real Time       │ │ ⏰ Attendance      │         │  │
│  │  │ Data Search        │ │ Tracker            │         │  │
│  │  │ Search & Suggest   │ │ Manage Attendance  │         │  │
│  │  └────────────────────┘ └────────────────────┘         │  │
│  │  ┌────────────────────┐ ┌────────────────────┐         │  │
│  │  │ 📚 View All        │ │ 📱 QR Code         │         │  │
│  │  │ Syllabus           │ │ Scanner            │         │  │
│  │  │ PDF/JSON/CSV       │ │ Generate & Download│         │  │
│  │  └────────────────────┘ └────────────────────┘         │  │
│  │                                                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## কম্পোনেন্ট হায়ারার্কি

```
ModernDashboard (Main Page)
├── Navbar
│   ├── Logo
│   ├── Title
│   ├── MenuToggle
│   ├── ThemeToggle
│   ├── Notifications
│   ├── Profile
│   └── Logout
│
├── Sidebar
│   ├── NavLinks
│   ├── Settings
│   └── Help
│
└── MainContent
    ├── WelcomeSection
    │   └── UserGreeting
    │
    ├── StatsCards (4)
    │   ├── CoursesCard
    │   ├── SubjectsCard
    │   ├── AttendanceCard
    │   └── ProgressCard
    │
    └── DashboardGrid (2x2)
        ├── RealTimeSearch
        │   ├── SearchInput
        │   ├── CategoryFilter
        │   ├── SuggestionsList
        │   └── ResultsList
        │
        ├── AttendanceTracker
        │   ├── StatsCards
        │   ├── AddSubjectForm
        │   └── SubjectsList
        │
        ├── SyllabusViewer
        │   ├── CourseSelector
        │   ├── SyllabusPreview
        │   └── DownloadButtons
        │
        └── QRCodeScanner
            ├── QRGenerator
            ├── QRPreview
            └── DownloadOptions
```

---

## ডেটা ফ্লো ডায়াগ্রাম

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   COMPONENT STATE                           │
│  • activeSection (null, 'search', 'attendance', etc)       │
│  • loading (boolean)                                        │
│  • error (string)                                           │
│  • data (object)                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    API REQUESTS                             │
│  • courseAPI.getAll()                                       │
│  • semesterAPI.getAll()                                     │
│  • subjectAPI.getAll()                                      │
│  • syllabusAPI.getCourse(id)                                │
│  • searchAPI.searchCourses(query)                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   BACKEND API                               │
│  • GET /api/courses                                         │
│  • GET /api/semesters                                       │
│  • GET /api/subjects                                        │
│  • GET /api/syllabus/course/<id>                            │
│  • GET /api/search/courses?q=<query>                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   DATABASE                                  │
│  • Courses Table                                            │
│  • Subjects Table                                           │
│  • Semesters Table                                          │
│  • Units Table                                              │
│  • SubUnits Table                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## স্টেট ম্যানেজমেন্ট

```
ModernDashboard State:
├── sidebarOpen: boolean
├── activeSection: null | 'search' | 'attendance' | 'syllabus' | 'qrcode'
├── loading: boolean
├── error: string | null
└── user: {
    id, name, email, role, ...
}

RealTimeSearch State:
├── searchQuery: string
├── results: array
├── suggestions: array
├── loading: boolean
├── error: string | null
└── selectedCategory: 'all' | 'courses' | 'subjects' | 'semesters'

AttendanceTracker State:
├── subjects: array
├── attendance: {
│   [subjectId]: {
│       present: number,
│       total: number,
│       subject: string
│   }
├── loading: boolean
└── error: string | null

SyllabusViewer State:
├── courses: array
├── selectedCourse: object | null
├── syllabusData: object | null
├── loading: boolean
└── error: string | null

QRCodeScanner State:
├── scannedData: object | null
├── qrValue: string
├── loading: boolean
├── error: string | null
└── showQR: boolean
```

---

## API এন্ডপয়েন্ট ম্যাপিং

```
┌────────────────────────────────────────────────────────────┐
│                    API ENDPOINTS                           │
├────────────────────────────────────────────────────────────┤
│                                                            │
│ Real Time Search:                                          │
│ ├── GET /api/courses                                       │
│ ├── GET /api/semesters                                     │
│ ├── GET /api/subjects                                      │
│ ├── GET /api/search/courses?q=<query>                      │
│ ├── GET /api/search/subjects?q=<query>                     │
│ └── GET /api/search/semesters?q=<query>                    │
│                                                            │
│ Attendance Tracker:                                        │
│ └── Local State Management (No API calls)                  │
│                                                            │
│ Syllabus Viewer:                                           │
│ ├── GET /api/courses                                       │
│ └── GET /api/syllabus/course/<course_id>  [NEW]            │
│                                                            │
│ QR Code Scanner:                                           │
│ └── Local QR Generation (No API calls)                     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## ফাইল স্ট্রাকচার

```
frontend/src/
├── pages/
│   ├── ModernDashboard.jsx          [MAIN PAGE]
│   ├── Home.jsx
│   ├── Login.jsx
│   ├── Register.jsx
│   └── ...
│
├── components/
│   ├── RealTimeSearch.jsx           [BUTTON 1]
│   ├── AttendanceTracker.jsx         [BUTTON 2]
│   ├── SyllabusViewer.jsx            [BUTTON 3]
│   ├── QRCodeScanner.jsx             [BUTTON 4]
│   ├── Navbar.jsx
│   ├── Sidebar.jsx
│   └── ...
│
├── utils/
│   ├── api.js                        [UPDATED]
│   └── ...
│
└── store/
    └── authStore.js

backend/app/
├── routes/
│   ├── syllabus.py                   [UPDATED]
│   ├── courses.py
│   ├── subjects.py
│   └── ...
│
├── models/
│   ├── course.py
│   ├── subject.py
│   └── ...
│
└── utils/
    ├── pdf_generator.py
    └── qr_generator.py
```

---

## প্রযুক্তি স্ট্যাক

```
Frontend:
├── React 18.2.0
├── React Router 6.20.0
├── Framer Motion 10.16.0
├── Tailwind CSS 3.3.0
├── Lucide React 0.292.0
├── Axios 1.6.0
├── jsPDF 2.5.1
├── jsPDF-AutoTable 3.5.31
├── QRCode.react 3.1.0
└── Zustand 4.4.0

Backend:
├── Flask 2.x
├── Flask-JWT-Extended 4.5.2
├── SQLAlchemy 2.0.44
├── PostgreSQL/SQLite
└── Python 3.x
```

---

## ডেটা ফ্লো উদাহরণ

### Real Time Search:
```
User Types "Computer"
    ↓
SearchInput onChange Event
    ↓
Debounce Timer (300ms)
    ↓
performSearch() Function
    ↓
API Calls:
├── courseAPI.getAll()
├── semesterAPI.getAll()
└── subjectAPI.getAll()
    ↓
Filter Results
    ↓
Update State:
├── results (20 items max)
└── suggestions (5 items max)
    ↓
Render Results Grid
```

### Attendance Tracker:
```
User Clicks "Add New Subject"
    ↓
Show Add Form
    ↓
User Enters Subject Name
    ↓
User Clicks "Add"
    ↓
Update State:
attendance[newId] = {
    present: 0,
    total: 40,
    subject: "Mathematics"
}
    ↓
Render New Subject Card
    ↓
User Updates Present/Total
    ↓
Calculate Percentage:
(present / total) * 100
    ↓
Update Progress Bar Color
```

### Syllabus Viewer:
```
User Selects Course
    ↓
syllabusAPI.getCourse(courseId)
    ↓
Backend Query:
SELECT * FROM courses WHERE id = courseId
SELECT * FROM subjects WHERE course_id = courseId
    ↓
Return Response:
{
    id, name, code, credits, duration,
    subjects: [...]
}
    ↓
Display Syllabus Preview
    ↓
User Clicks "Download PDF"
    ↓
Generate PDF with jsPDF
    ↓
Create Table with AutoTable
    ↓
Download File
```

### QR Code Scanner:
```
User Enters Data
    ↓
User Clicks "Generate QR"
    ↓
QRCode.react Component
    ↓
Generate QR Image
    ↓
Display QR Preview
    ↓
User Clicks "Download QR"
    ↓
Convert Canvas to PNG
    ↓
Trigger Download
```

---

## সিকিউরিটি ফ্লো

```
┌─────────────────────────────────────────────────────────┐
│                   USER REQUEST                         │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              JWT TOKEN VALIDATION                       │
│  • Check localStorage for token                         │
│  • Add to Authorization header                          │
│  • Validate on backend                                  │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              CORS VALIDATION                            │
│  • Check origin                                         │
│  • Allow credentials                                    │
│  • Set allowed headers                                  │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              INPUT VALIDATION                           │
│  • Sanitize query strings                               │
│  • Validate data types                                  │
│  • Check data ranges                                    │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              DATABASE QUERY                             │
│  • Use parameterized queries                            │
│  • Prevent SQL injection                                │
│  • Filter by user ID                                    │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              RESPONSE VALIDATION                        │
│  • Check response status                                │
│  • Handle errors gracefully                             │
│  • Log security events                                  │
└─────────────────────────────────────────────────────────┘
```

---

## পারফরম্যান্স অপটিমাইজেশন

```
Frontend:
├── Code Splitting
│   └── Lazy load components
├── Debouncing
│   └── Search input (300ms)
├── Memoization
│   └── Prevent unnecessary re-renders
├── Image Optimization
│   └── Use SVG for icons
└── Caching
    └── Store API responses

Backend:
├── Database Indexing
│   └── Index frequently searched columns
├── Query Optimization
│   └── Use efficient SQL queries
├── Response Caching
│   └── Cache static data
└── Pagination
    └── Limit results (20 max)
```

---

**আর্কিটেকচার ডকুমেন্ট**
**সর্বশেষ আপডেট**: ডিসেম্বর ২০২৫
**স্ট্যাটাস**: ✅ সম্পূর্ণ
